export PYTHONDONTWRITEBYTECODE := "1"

# === SERVER ===

dev-server:
    uv run fastapi dev main.py

server:
    uv run fastapi run main.py

# === TESTS ===

test:
    uv run --group dev pytest -v

test-route-optimizer:
    uv run --group dev pytest -v tests/test_route_optimizer.py tests/test_lazy_matrix.py

extract-places count="50":
    uv run --group scripts python scripts/extract_places.py --count {{count}}