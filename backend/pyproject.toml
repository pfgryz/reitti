[project]
name = "reitti-backend"
version = "0.1.0"
description = "API server for the Reitti project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.136.0",
]
