[project]
name = "reitti-backend"
version = "0.1.0"
description = "API server for the Reitti project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.136.0",
    "httpx>=0.28.1",
    "pydantic>=2.13.3",
]
